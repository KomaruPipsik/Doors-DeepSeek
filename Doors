--// DOORS SCRIPT - FULL OPTIMIZED & FIXED VERSION
--// Services
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local StarterGui = game:GetService("StarterGui")

local Player = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local Character = Player.Character or Player.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")

Player.CharacterAdded:Connect(function(char)
	Character = char
	Humanoid = char:WaitForChild("Humanoid")
	HumanoidRootPart = char:WaitForChild("HumanoidRootPart")
end)

--// GUI Creation
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DoorsGui"
ScreenGui.ResetOnSpawn = false
if syn and syn.protect_gui then
	syn.protect_gui(ScreenGui)
	ScreenGui.Parent = CoreGui
else
	pcall(function()
		ScreenGui.Parent = CoreGui
	end)
	if ScreenGui.Parent ~= CoreGui then
		ScreenGui.Parent = Player:WaitForChild("PlayerGui")
	end
end

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 520, 0, 420)
MainFrame.Position = UDim2.new(0.5, -260, 0.5, -210)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Title.Text = "DOORS UTILITY MENU (Press RightShift to Open/Close)"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 13
Title.Font = Enum.Font.SourceSansBold
Title.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 8)
TitleCorner.Parent = Title

local ScrollingFrame = Instance.new("ScrollingFrame")
ScrollingFrame.Size = UDim2.new(1, -20, 1, -60)
ScrollingFrame.Position = UDim2.new(0, 10, 0, 50)
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 1400)
ScrollingFrame.ScrollBarThickness = 6
ScrollingFrame.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 8)
UIListLayout.Parent = ScrollingFrame

--// Notification System
local function SendNotification(title, text)
	pcall(function()
		StarterGui:SetCore("SendNotification", {
			Title = title,
			Text = text,
			Duration = 3
		})
	end)
end

--// UI Toggle
UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if input.KeyCode == Enum.KeyCode.RightShift then
		MainFrame.Visible = not MainFrame.Visible
	end
end)

--// Universal Helper to create Billboard ESP (Optimized Cache)
local espCache = {}
local function CreateOrUpdateESP(parent, nameTag, textStr, textColor)
	if not parent then return end
	local targetPart = parent:IsA("BasePart") and parent or parent:FindFirstChildWhichIsA("BasePart")
	if not targetPart then return end
	
	local bill = espCache[targetPart]
	if not bill or not bill.Parent then
		bill = targetPart:FindFirstChild(nameTag)
		if not bill then
			bill = Instance.new("BillboardGui")
			bill.Name = nameTag
			bill.Size = UDim2.new(0, 110, 0, 45)
			bill.AlwaysOnTop = true
			bill.StudsOffset = Vector3.new(0, 2.5, 0)
			
			local txt = Instance.new("TextLabel")
			txt.Name = "Text"
			txt.Size = UDim2.new(1, 0, 1, 0)
			txt.BackgroundTransparency = 1
			txt.Text = textStr
			txt.TextColor3 = textColor
			txt.TextStrokeTransparency = 0
			txt.TextSize = 15
			txt.Font = Enum.Font.SourceSansBold
			txt.Parent = bill
			
			bill.Parent = targetPart
		end
		espCache[targetPart] = bill
	end
end

--// UI Builder Helpers
local function CreateToggle(name, callback)
	local ToggleBtn = Instance.new("TextButton")
	ToggleBtn.Size = UDim2.new(1, 0, 0, 35)
	ToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
	ToggleBtn.Text = name .. ": OFF"
	ToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
	ToggleBtn.TextSize = 14
	ToggleBtn.Font = Enum.Font.SourceSansBold
	ToggleBtn.Parent = ScrollingFrame
	
	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 6)
	Corner.Parent = ToggleBtn
	
	local state = false
	ToggleBtn.MouseButton1Click:Connect(function()
		state = not state
		if state then
			ToggleBtn.Text = name .. ": ON"
			ToggleBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
		else
			ToggleBtn.Text = name .. ": OFF"
			ToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
		end
		callback(state)
	end)
end

local function CreateSlider(name, min, max, default, callback)
	local Container = Instance.new("Frame")
	Container.Size = UDim2.new(1, 0, 0, 50)
	Container.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
	Container.Parent = ScrollingFrame
	
	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 6)
	Corner.Parent = Container
	
	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(1, 0, 0, 20)
	Label.BackgroundTransparency = 1
	Label.Text = name .. ": " .. tostring(default)
	Label.TextColor3 = Color3.fromRGB(255, 255, 255)
	Label.TextSize = 14
	Label.Font = Enum.Font.SourceSansBold
	Label.Parent = Container
	
	local SliderBar = Instance.new("TextButton")
	SliderBar.Size = UDim2.new(0.9, 0, 0, 10)
	SliderBar.Position = UDim2.new(0.05, 0, 0, 30)
	SliderBar.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
	SliderBar.Text = ""
	SliderBar.Parent = Container
	
	local SliderCorner = Instance.new("UICorner")
	SliderCorner.CornerRadius = UDim.new(0, 4)
	SliderCorner.Parent = SliderBar
	
	local Fill = Instance.new("Frame")
	Fill.Size = UDim2.new((default - min)/(max - min), 0, 1, 0)
	Fill.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
	Fill.BorderSizePixel = 0
	Fill.Parent = SliderBar
	
	local FillCorner = Instance.new("UICorner")
	FillCorner.CornerRadius = UDim.new(0, 4)
	FillCorner.Parent = Fill
	
	local dragging = false
	SliderBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = true
		end
	end)
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = false
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
			local mousePos = UserInputService:GetMouseLocation().X
			local barPos = SliderBar.AbsolutePosition.X
			local barSize = SliderBar.AbsoluteSize.X
			local percent = math.clamp((mousePos - barPos)/barSize, 0, 1)
			Fill.Size = UDim2.new(percent, 0, 1, 0)
			local val = math.floor(min + (max - min) * percent)
			Label.Text = name .. ": " .. tostring(val)
			callback(val)
		end
	end)
end

--// FEATURES TOGGLES //--
local doorsESPEnabled = false
CreateToggle("Doors ESP (Correct & Dupe)", function(state) doorsESPEnabled = state end)

local rushAmbushESP = false
local rushNotifications = false
CreateToggle("Rush & Ambush ESP", function(state) rushAmbushESP = state end)
CreateToggle("Rush & Ambush Notifications", function(state) rushNotifications = state end)

local fullBrightEnabled = false
CreateToggle("Full Bright", function(state)
	fullBrightEnabled = state
	if state then
		Lighting.Brightness = 2
		Lighting.ClockTime = 14
		Lighting.GlobalShadows = false
		Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
	else
		Lighting.GlobalShadows = true
	end
end)

-- Boost FPS Feature
local boostFpsEnabled = false
CreateToggle("Boost FPS", function(state)
	boostFpsEnabled = state
	if state then
		for _, v in ipairs(Workspace:GetDescendants()) do
			if v:IsA("BasePart") then
				v.Material = Enum.Material.SmoothPlastic
				v.Reflectance = 0
			elseif v:IsA("Decal") or v:IsA("Texture") then
				v.Transparency = 1
			end
		end
		Lighting.GlobalShadows = false
		Lighting.FogEnd = 999999
	else
		Lighting.GlobalShadows = true
	end
end)

local keyESPEnabled = false
CreateToggle("Key ESP", function(state) keyESPEnabled = state end)

local leverESPEnabled = false
CreateToggle("Lever For Gate ESP", function(state) leverESPEnabled = state end)

local customSpeed = 16
local speedEnabled = false
local antiTpEnabled = false
CreateToggle("Enable Custom Speed", function(state) speedEnabled = state end)
CreateSlider("Speed Value", 16, 40, 16, function(val) customSpeed = val end)
CreateToggle("Anti-Teleport / Safe Speed", function(state) antiTpEnabled = state end)

local eyesESP = false
local eyesAntiDamage = false
CreateToggle("Eyes ESP", function(state) eyesESP = state end)
CreateToggle("Eyes Anti-Damage (Smart)", function(state) eyesAntiDamage = state end)

local screechAutoTurn = false
local screechNotif = false
CreateToggle("Screech Notifications", function(state) screechNotif = state end)
CreateToggle("Screech Auto-Turn Camera", function(state) screechAutoTurn = state end)

local figureESP = false
local figureAvoid = false
local avoidRadius = 15
CreateToggle("Figure ESP", function(state) figureESP = state end)
CreateToggle("Figure Avoidance (Smart Height)", function(state) figureAvoid = state end)
CreateSlider("Figure Avoid Radius", 10, 40, 15, function(val) avoidRadius = val end)

local booksESP = false
CreateToggle("Room 50 Books ESP", function(state) booksESP = state end)

local snareESP = false
CreateToggle("Snares ESP (90-99)", function(state) snareESP = state end)

local polesESP = false
CreateToggle("Room 100 Breaker Poles ESP", function(state) polesESP = state end)


--// OPTIMIZED THROTTLED ENGINE LOOP (Runs every 0.2 seconds instead of every frame to eliminate lags)
local notifiedRush = {}
local clockTimer = 0

RunService.RenderStepped:Connect(function(dt)
	clockTimer = clockTimer + dt
	if clockTimer < 0.2 then return end
	clockTimer = 0

	local currentRooms = Workspace:FindFirstChild("CurrentRooms")
	
	-- Global Check for Dangerous Entities (Rush, Ambush, Screech) to safely disable Eyes anti-damage
	local dangerActive = false
	for _, obj in ipairs(Workspace:GetChildren()) do
		if obj.Name == "RushMoving" or obj.Name == "AmbushMoving" or obj.Name == "Eyes" then
			dangerActive = true
			break
		end
	end
	for _, name in ipairs({"Screech", "ScreechRetro", "ScreechRushMode"}) do
		if Workspace:FindFirstChild(name, true) then
			dangerActive = true
			break
		end
	end

	-- Rooms Scanning
	if currentRooms then
		for _, room in ipairs(currentRooms:GetChildren()) do
			-- 1. Doors ESP
			if doorsESPEnabled then
				local door = room:FindFirstChild("Door", true)
				if door then
					local isDupe = door:FindFirstChild("dupeDoor") or door.Name == "dupeDoor"
					CreateOrUpdateESP(door, "DoorsESP", isDupe and "DUPE DOOR" or "CORRECT DOOR", isDupe and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(0, 255, 0))
				end
			end
			
			-- 2. Lever ESP
			if leverESPEnabled then
				local assets = room:FindFirstChild("Assets")
				if assets then
					for _, item in ipairs(assets:GetDescendants()) do
						if item.Name == "LeverForGate" then
							CreateOrUpdateESP(item, "LeverESP", "LEVER", Color3.fromRGB(255, 255, 0))
						end
					end
				end
			end
			
			-- 3. Key ESP
			if keyESPEnabled then
				local assets = room:FindFirstChild("Assets")
				if assets then
					for _, dresser in ipairs(assets:GetDescendants()) do
						if dresser.Name == "Dresser" or dresser.Name == "DrawerContainer" then
							local keyObtain = dresser:FindFirstChild("KeyObtain", true)
							if keyObtain then
								local hit = keyObtain:FindFirstChild("Hitbox") or keyObtain:FindFirstChild("KeyHitbox") or keyObtain
								CreateOrUpdateESP(hit, "KeyESP", "KEY", Color3.fromRGB(255, 255, 0))
							end
						end
					end
				end
			end
			
			-- 4. Room 50 Books ESP
			if booksESP then
				local assets = room:FindFirstChild("Assets")
				if assets then
					for _, shelf in ipairs(assets:GetDescendants()) do
						if shelf.Name == "Modular_Bookshelf" then
							local liveBook = shelf:FindFirstChild("LiveHintBook", true)
							if liveBook then
								CreateOrUpdateESP(liveBook, "BookESP", "BOOK", Color3.fromRGB(0, 255, 255))
							end
						end
					end
				end
			end
			
			-- 5. Snares ESP (90-99)
			if snareESP then
				local assets = room:FindFirstChild("Assets")
				if assets then
					for _, snareModel in ipairs(assets:GetDescendants()) do
						if snareModel.Name == "Snare" then
							local base = snareModel:FindFirstChild("SnareBase") or snareModel
							CreateOrUpdateESP(base, "SnareESP", "TRAP", Color3.fromRGB(255, 128, 0))
						end
					end
				end
			end
			
			-- 6. Room 100 Breaker Poles ESP
			if polesESP then
				for _, pole in ipairs(room:GetDescendants()) do
					if pole.Name == "LiveBreakerPolePickup" then
						CreateOrUpdateESP(pole, "PoleESP", "LEVER", Color3.fromRGB(0, 255, 0))
					end
				end
			end
			
			-- 7. Figure ESP & Smart Avoidance (Height/Climb Check if cornered)
			local rig = room:FindFirstChild("FigureRig", true) or room:FindFirstChild("FigureSetup", true)
			if rig then
				local primaryPart = rig:FindFirstChild("HumanoidRootPart") or rig:FindFirstChildWhichIsA("BasePart")
				if primaryPart then
					if figureESP then
						CreateOrUpdateESP(primaryPart, "FigureESP", "FIGURE", Color3.fromRGB(255, 0, 0))
					end
					if figureAvoid and Humanoid and HumanoidRootPart then
						local dist = (HumanoidRootPart.Position - primaryPart.Position).Magnitude
						if dist < avoidRadius then
							-- Check if player is stuck/colliding against a wall (dead end) by checking velocity movement drop
							local raycastParams = RaycastParams.new()
							raycastParams.FilterType = RaycastFilterType.Exclude
							raycastParams.FilterDescendantsInstances = {Character}
							
							local hitForward = Workspace:Raycast(HumanoidRootPart.Position, HumanoidRootPart.CConsole or (HumanoidRootPart.CFrame.LookVector * 4), raycastParams)
							if hitForward or Humanoid.MoveDirection.Magnitude < 0.1 then
								-- Dead end / Cornered: Jump/Elevate upward so Figure cannot reach you!
								Humanoid.Jump = true
							else
								local dir = (HumanoidRootPart.Position - primaryPart.Position).Unit
								Humanoid:Move(dir * 2)
							end
						end
					end
				end
			end
		end
	end
	
	-- Global Entities (Rush & Ambush)
	for _, obj in ipairs(Workspace:GetChildren()) do
		if obj.Name == "RushMoving" or obj.Name == "AmbushMoving" then
			if rushNotifications and not notifiedRush[obj] then
				notifiedRush[obj] = true
				SendNotification("WARNING!", obj.Name .. " is incoming!")
			end
			if rushAmbushESP then
				CreateOrUpdateESP(obj, "RushAmbushESP", obj.Name:upper(), Color3.fromRGB(255, 50, 50))
			end
		end
		
		-- Eyes ESP & Smart Anti-Damage (Turns off automatically if Rush/Ambush/Screech are active)
		if obj.Name == "Eyes" then
			if eyesESP then
				CreateOrUpdateESP(obj, "EyesESP", "EYES", Color3.fromRGB(150, 0, 255))
			end
			if eyesAntiDamage and not dangerActive and HumanoidRootPart then
				Camera.CFrame = CFrame.new(Camera.CFrame.Position, Vector3.new(Camera.CFrame.Position.X, Camera.CFrame.Position.Y - 10, Camera.CFrame.Position.Z + 10))
			end
		end
	end
	
	-- Screech Global Handler
	for _, entityName in ipairs({"Screech", "ScreechRetro", "ScreechRushMode"}) do
		local screech = Workspace:FindFirstChild(entityName, true)
		if screech then
			if screechNotif and not screech:GetAttribute("Notified") then
				screech:SetAttribute("Notified", true)
				SendNotification("SCREECH!", "Screech has appeared!")
			end
			if screechAutoTurn then
				local part = screech:FindFirstChild("HumanoidRootPart") or screech:FindFirstChildWhichIsA("BasePart")
				if part then
					Camera.CFrame = CFrame.new(Camera.CFrame.Position, part.Position)
				end
			end
		end
	end
end)

-- Speed & Anti-Teleport Loop
local lastPosition = Vector3.new()
RunService.Stepped:Connect(function()
	if speedEnabled and Humanoid and HumanoidRootPart then
		Humanoid.WalkSpeed = customSpeed
		if antiTpEnabled then
			if (HumanoidRootPart.Position - lastPosition).Magnitude > 50 and not Workspace:GetAttribute("InElevator") then
				HumanoidRootPart.CFrame = CFrame.new(lastPosition)
			else
				lastPosition = HumanoidRootPart.Position
			end
		else
			lastPosition = HumanoidRootPart.Position
		end
	end
end)

SendNotification("Doors Script Loaded", "Optimized version ready. Press RightShift to toggle menu.")
